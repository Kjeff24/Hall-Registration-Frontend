import 'package:flutter/material.dart';

class RegMainPage extends StatelessWidget {
  const RegMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}